yawn tzku vevy mhul
miko58464@gmail.com
amhashif@gmail.com
jems@gmail.com

-post
-follow and suggestion
-

DELETE FROM followers WHERE email='sofoniyaskediryesuf@gmail.com'
AND followeremail = 'jems@gmail.com'

<span commentid="1729598651253-amha_123-283906505" onclick="seeReplay(event)" class="text-gray-400  cursor-pointer font-sans font-semibold text-sm">View Replay</span>

SELECT email, COUNT(followeremail) AS follower_count
FROM followers
GROUP BY email
ORDER BY follower_count DESC
LIMIT 1;


<div class="flex item-center mt-4 mb-4">
        <img style="height: 50px;width:50px" class="rounded-full" src="/image/miko58464@gmail.com/1729598600386-977621604-porofile 2.jpg" alt="profile">
        <div class="ml-3 bo">
            <p class="text-white font-normal text-sm">LeoMess Lorem ipsum dolor sit amet consectetur adipisicing elit. Minus inventore dolorem laudantium vitae expedita explicabo unde a fuga aliquam iusto minima illo, animi nostrum reiciendis iure, atque nobis deleniti sit.i</p>
            <div class="flex justify-between mt-2" style="width: 30%;">
                <span class="text-gray-400 cursor-pointer font-sans font-semibold text-sm">1d</span>
                <span class="text-gray-400 cursor-pointer font-sans font-semibold text-sm">10 like</span>
                <span class="text-gray-400 cursor-pointer font-sans font-semibold text-sm">Replay</span>
            </div>
        </div>
</div>


SELECT * FROM users
WHERE similarity(username, 'soss')>0.001 OR
similarity(fullname,'solse')>0.001 OR similarity(bio,'sosd') >0.001 OR
similarity(email,'sosd')>0.001 
ORDER BY similarity(username, 'so') DESC;

SELECT u.*, COUNT(f.followeremail) AS follower_count
FROM users u
LEFT JOIN followers f ON f.email = u.email
WHERE similarity(u.username, 'so') > 0.001 
   OR similarity(u.fullname, 'so') > 0.001 
   OR similarity(u.bio, 'so') > 0.001 
   OR similarity(u.email, 'so') > 0.001
GROUP BY u.username, u.fullname, u.bio, u.email,u.id  -- Add any other columns as necessary
ORDER BY similarity(u.username, 'so') DESC, follower_count DESC;


function commentItration(data){
  data.forEach(item => {
    const mainDiv = document.createElement("div");
    mainDiv.classList.add("flex", "items-center", "mt-4", "mb-4");
  
    // Create and set the image element
    const img = document.createElement("img");
    img.classList.add("rounded-full");
    img.src = item.profile_image;
    img.alt = "profile";
    img.style.height = "50px";
    img.style.width = "50px";
  
    // Create the inner div for text and actions
    const innerDiv = document.createElement("div");
    innerDiv.classList.add("ml-3", "bo");
  
    // Create and set the paragraph element for the text
    const textPara = document.createElement("p");
    textPara.style.width='80%';
    textPara.classList.add("text-white", "font-normal", "text-sm");
    textPara.textContent = `${item.fullname}   ${item.usercomment}`;
  
    // Create the actions div
    const actionsDiv = document.createElement("div");
    actionsDiv.classList.add("flex", "justify-between", "mt-4");
    actionsDiv.style.width = "100px";
  
    // Time ago span
    const timeSpan = document.createElement("span");
    timeSpan.classList.add("text-gray-400", "cursor-pointer", "font-sans", "font-semibold", "text-sm");
    timeSpan.textContent = '1d';
  
    // Likes span
    const likesSpan = document.createElement("span");
    likesSpan.classList.add("text-gray-400", "cursor-pointer", "font-sans", "font-semibold", "text-sm");
    likesSpan.textContent = '10';
  
    // Reply span
    const replySpan = document.createElement("span");
    replySpan.classList.add("text-gray-400", "cursor-pointer", "font-sans", "font-semibold", "text-sm");
    replySpan.textContent = "Reply";
  
    // Append the spans to the actions div
    actionsDiv.append(timeSpan, likesSpan, replySpan);
  
    // Append the paragraph and actions div to the inner div
    innerDiv.append(textPara, actionsDiv);
  
    // Append the image and inner div to the main div
    mainDiv.append(img, innerDiv);
  
    // Append the main div to the container
    commentSession.appendChild(mainDiv);
  });
}


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>Instagram</title>
    <link rel="stylesheet" href="output.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="https://static.xx.fbcdn.net/rsrc.php/v3/yQ/r/SOrOnJCceiE.png" type="image/x-icon" style="border-radius: 50%;">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <style>   
        ::-webkit-scrollbar {
          scrollbar-width: thin;
          border-radius: 20px;
        }
        
        /* Track */
        ::-webkit-scrollbar-track {
          background: #000000;
        }
        
        /* Handle */
        ::-webkit-scrollbar-thumb {
          background: rgba(40,40,40,1);;
        }
        
        /* Handle on hover */
        ::-webkit-scrollbar-thumb:hover {
          background: #555;
        }
        .active{
          border: 2px dashed #ccc;
        }
    </style>

  </head>






Incoming message

    <div class="flex items-start space-x-2">
          <div class="w-8 h-8 bg-gray-400 rounded-full"></div>
          <div class="bg-gray-700 p-3 rounded-lg max-w-xs">
            <p>Mn Demo sira jemerik?😂🤣</p>
          </div>
        </div>
        <p class="text-sm text-gray-500 ml-10">Oct 25, 2024 7:39 AM</p>

 out going message

      <div class="flex justify-end">
          <div class="bg-blue-600 p-3 rounded-lg max-w-xs">
            <p>Bakh tewew wedeh nw 😊🙃</p>
          </div>
        </div>





        const callerInfoDialog = `
         <img src="${callerInfo.profile_image}" class=" w-20 h-20 rounded-full" />
    <p>${callerInfo.username}</p>
    <div class="callButtons flex justify-between">
      <button id="acceptVideoCall" class="w-28 cursor-pointer h-16 bg-green-500 rounded" style="display: grid;place-items:center;z-index:100">
        <img src="/image/videocall/rejectCall.png" class="h-12 w-20" alt="" srcset="">
      </button>
      <button  class="w-28 cursor-pointer h-16 bg-red-600 rounded" style="display: grid;place-items:center">
        <img src="/image/videocall/acceptCall.png" class="h-12 w-20" alt="" srcset="">
      </button>
    </div>
    <script>
      document.getElementById("acceptVideoCall").addEventListener("click",()=>{
        console.log("hello")
              acceptCallHandler();
      })
    </script>
    `